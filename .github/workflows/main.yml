name: Farhan Ali

on:
  schedule:
    - cron: "55 12 * * *"

jobs:
  scheduling-workflow:
   runs-on: ubuntu-latest

   steps:
    - name: Schedule
      run: echo "Scheduled Github Actions Workflow"
