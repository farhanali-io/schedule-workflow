name: Farhan Ali

on:
  schedule:
    - cron: "17 20 * * *"
    

jobs:
  scheduling-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: Schedule
        run: echo "Scheduled Github Actions Workflow"
